# 热讯工坊 Showboard

## 1. 问题（假设）

### 1.1 项目背景问题
- 在短视频内容生产场景中，个人创作者常见瓶颈是：
  - 热点信息收集碎片化，选题效率低。
  - 营销号内容过多、转载链路复杂，新闻三要素（真实性、时效性、重要性）在实际执行中难以稳定保障。
  - 爆款规律分析依赖经验，难以稳定复用。
  - 脚本、剪辑、发布协作链路长，单人执行成本高。
  - 从选题到发布的过程难以标准化与追踪。

### 1.2 核心假设
- 假设 A：将内容生产拆分为多个明确岗位（热点监控、爆款分析、文案、剪辑、运营），可显著降低单人创作的认知负担。
- 假设 B：基于 Agent 分工 + 工作流编排，比“单一聊天式 AI”更适合稳定产出短视频方案。
- 假设 C：即使缺少在线大模型密钥，使用本地模板回退机制也能先跑通流程与产物结构。
- 假设 D：通过独立 Workspace 按岗位写入产物，可提升过程可追溯性与复盘效率。
- 假设 E：在热点筛选和分析环节引入真实性核实与风险提示，可降低营销号噪声导致的误判与失真传播。

### 1.3 业务目标（从仓库实现推断）
- 目标 1：构建“热点监控 -> 爆款分析 -> 脚本生成 -> 剪辑方案 -> 运营发布”的闭环。
- 目标 2：支持老板输入选题方向、受众与时长，快速生成可执行内容方案。
- 目标 3：在本地完成可视化调度、服务控制与结果归档。

---

## 2. 实践方法与过程

### 2.1 总体方法
- 技术架构：
  - 后端：Python + FastAPI。
  - 前端：React + Vite。
- 实现策略：
  - 通过后端 API 封装工作流步骤。
  - 通过前端调度台将输入、执行、结果可视化。
  - 通过运行目录与岗位目录保存产物文档。

### 2.2 工作流过程（后端）
- 入口能力：
  - `POST /api/workflows/hot-video`：执行完整热视频工作流。
  - `POST /api/topics/scout`：只做热点扫描。
  - `POST /api/scripts/generate`：只生成脚本。
- 流程步骤（`backend/app/workflows.py`）：
  1. 根据老板输入（领域、方向、受众、时长）启动运行实例。
  2. 热点监控员输出候选选题（title/heat/source_hint/angle/risk），并在风险字段中给出事实核查/真实性相关提示。
  3. 爆款分析师对首选主题进行传播结构分析。
  4. 文案助手生成 90-120 秒口播脚本。
  5. 视频剪辑员输出 MoneyPrinterTurbo 执行级剪辑方案。
  6. 运营大师输出标题、封面、发布时间、评论引导与复盘指标。
  7. 将全流程摘要写入 `run-summary.json`，并回传工作流状态。

### 2.3 回退与稳定性设计
- 若模型不可用或返回异常：
  - 热点、分析、脚本、剪辑、运营均有 fallback 模板。
- 输出安全边界：
  - 热点数据解析失败时自动回退默认话题集合。
- 状态记录：
  - 内存中维护运行状态（queued/running/completed/failed）。

### 2.4 数据组织与产物沉淀
- Agent 工作区：`workspaces/agents/<agent_id>`。
- 运行产物目录：`workspaces/runs/<run_id>/<agent_id>/<agent_id>.md`。
- 摘要文件：`workspaces/runs/<run_id>/boss/run-summary.json`。

### 2.5 前端实践过程
- 调度台模块：总览、热点雷达、脚本工坊、剪辑队列、员工、设置。
- 用户操作流程：
  1. 填写老板指令（领域/方向/受众/时长）。
  2. 启动完整工作流或仅生成脚本。
  3. 查看热点候选、阶段状态与产物详情。
  4. 在剪辑队列触发 MoneyPrinterTurbo 成片。
  5. 在设置页查看并控制本地前后端服务状态。

### 2.6 剪辑能力接入过程
- 已接入 Skill：MoneyPrinterTurbo 官方 Agent Skill。
- 状态接口：`GET /api/video/moneyprinterturbo/status`。
- 运行接口：`POST /api/video/moneyprinterturbo/run`。
- 环境依赖检查：
  - `MPT_LLM_PROVIDER`
  - `AI_API_KEY`
  - `AI_BASE_URL`
  - `AI_MODEL`
  - `MPT_PEXELS_API_KEY`

---

## 3. 数据与结果

### 3.1 输入数据结构
- 选题输入（TopicSeed）：
  - `domain`（领域）
  - `brief`（方向）
  - `audience`（受众）
  - `duration_seconds`（30-240 秒）
- 热点条目（Topic）：
  - `title`
  - `heat`（0-100）
  - `source_hint`
  - `angle`
  - `risk`

### 3.2 过程数据与输出数据
- 每个岗位输出统一结构（AgentOutput）：
  - `agent_id`、`agent_name`、`title`、`content`、`artifact_path`、`created_at`。
- 完整运行输出（WorkflowRun）：
  - 运行 ID、状态、输入种子、热点列表、全岗位产物、运行目录、时间戳、异常信息。

### 3.3 已有可确认结果（来自实现与文档）
- 结果 1：项目已实现完整 API 链路与前端调度界面。
- 结果 2：项目支持无 Key 模式（local-template）下的流程验证。
- 结果 3：项目已集成 MoneyPrinterTurbo Skill 路径与调用命令。
- 结果 4：项目产物支持按角色落盘，具备可追溯性。
- 结果 5：热点输出包含 `risk` 字段，且默认模板中已体现“事实核查”类风险提示。

### 3.4 缺失数据标记
- [缺少可用内容] 真实线上运行样本数量（运行次数、成功率、失败率）。
- [缺少可用内容] 量化质量指标（完播率、CTR、互动率、转化率）的实验数据。
- [缺少可用内容] 多轮 A/B 选题对比数据与统计显著性分析。
- [缺少可用内容] MoneyPrinterTurbo 真实成片耗时分布、失败原因占比。
- [缺少可用内容] 不同模型配置下输出质量与成本对比数据。

---

## 4. 讨论与结论

### 4.1 讨论：方案价值
- 岗位拆分使“任务边界”更清晰，利于单人管理。
- 输出标准化（结构化对象 + Markdown 产物）利于复盘与继续加工。
- 本地模板回退提升了可用性，降低初始接入门槛。

### 4.2 讨论：当前局限
- 工作流状态存储在内存，服务重启后历史状态不可持续查询。
- 默认流程选择首个热点，缺少自动化多候选评分与策略路由。
- 数据层偏向“流程可用”，缺少实验评估与运营闭环报表。
- 本地控制能力依赖特定运行环境（如 PowerShell 路径和端口检测机制）。
- 真实性核实目前以“风险提示”为主，尚未形成强制化、多信源交叉验证的硬性门禁流程。

### 4.3 结论
- 本项目已完成“一人公司内容生产线”的可运行原型，核心流程连通性良好。
- 当前阶段更偏“工程化验证 + 结构化产物沉淀”，而非“已验证效果最优的生产系统”。
- 若进入实战运营阶段，关键增量在于：
  - 建立真实业务数据采集与评估体系；
  - 强化多主题决策与自动复盘能力；
  - 稳定化成片链路与异常恢复机制。

---

## 5. 作品功能介绍

### 5.1 核心功能总览
- 热点选题：根据老板输入生成 3-5 个热点候选并附热度、线索与真实性相关风险提示。
- 爆款分析：输出传播结构、冲突点与标题方向建议。
- 脚本生成：产出 90-120 秒短视频脚本（含结构段落）。
- 剪辑方案：生成素材、画幅、字幕、配音、导出命令建议。
- 运营发布：生成标题、封面、发布时间与复盘指标。

### 5.2 调度台功能
- 总览页：流水线状态、热点、产物集中展示。
- 热点雷达：快速输入方向并触发扫描。
- 脚本工坊：单独生成脚本并查看输出。
- 剪辑队列：查看剪辑/运营队列并触发 MPT 成片。
- 员工页：查看岗位职责、技能来源、工作区路径。
- 设置页：查看 AI 配置与本地服务控制状态。

### 5.3 接口功能
- 健康与配置：`/api/health`、`/api/status`。
- 员工管理：`/api/agents`。
- 选题与脚本：`/api/topics/scout`、`/api/scripts/generate`。
- 工作流：`/api/workflows/hot-video`、`/api/workflows`、`/api/workflows/{run_id}`。
- 视频工具：`/api/video/moneyprinterturbo/status`、`/api/video/moneyprinterturbo/run`。

### 5.4 多岗位与技能能力
- 已装专项 Skill：
  - 视频剪辑员：moneyprinterturbo-video
  - 运营大师：newmedia-operations
  - 产品经理：Product-Manager-Skills
  - 心理疗愈师：mental-health-assistant
- 未装专项 Skill（当前用内置提示词）：
  - 爆款分析师、文案助手、程序员、股票助手

### 5.5 运行与部署相关说明
- 前端默认：`http://127.0.0.1:5173`
- 后端默认：`http://127.0.0.1:8017`
- API 文档：`http://127.0.0.1:8017/docs`
- 依赖密钥：`AI_API_KEY`（可选但影响实时能力）

---

## 6. 缺失信息总表（显式标记）

- [缺少可用内容] 真实生产环境部署拓扑与资源规格（CPU/GPU/内存）。
- [缺少可用内容] 历史版本迭代记录与关键里程碑对比数据。
- [缺少可用内容] 用户访谈、需求验证与可用性测试报告。
- [缺少可用内容] 版权与合规审查的正式流程文档（素材、音频、模型输出）。
- [缺少可用内容] 商业化指标（获客成本、变现路径、ROI）与财务测算。
